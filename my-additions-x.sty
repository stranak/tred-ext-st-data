% !TEX root = disertace.tex
%!TEX encoding = UTF-8 Unicode
% The latex packages I use over and over in most of my documents
% Pavel Stranak
%
%\usepackage{euler}
\usepackage{xltxtra} % automaticall loads fontspec and some other packages
\setmainfont[Mapping=tex-text]{Minion Pro}
\setsansfont[Mapping=tex-text]{Myriad Pro}
\setmonofont[Mapping=tex-text, Scale=MatchLowercase]{Consolas}
\setmathsf{Cambria Math}
% languages
\usepackage{polyglossia}
\setmainlanguage[variant=british]{english}
\setotherlanguage{czech}


\usepackage{url}
\usepackage{color} %pro korektury
%\usepackage[xetex]{color} %pro korektury
%\def\xxx#1{\textcolor[cmyk]{0,1,1,0}{#1}}
\def\xxx#1{\textcolor{red}{#1}}
\usepackage{rotating}
\usepackage{paralist} % for better itemize and enumerate
% asparaenum, inparaenum, \begin{compactenum}[{Example} a)] etc.
% Also, items can be referenced via \label{} and \ref{}
\usepackage{gb4e}
\usepackage{wrapfig}
\usepackage{array}

%%% ToC (table of contents) APPEARANCE
\usepackage[nottoc,notlof,notlot]{tocbibind} % Put the bibliography in the ToC

\usepackage[    
   pdfdisplaydoctitle, breaklinks, colorlinks, linkcolor=black,
   citecolor=black, filecolor=black, urlcolor=black, 
   backref, hyperfootnotes, xetex, unicode
   ]{hyperref} % backref asi nakonec zrusime 

\usepackage{natbib}
\bibpunct{(}{)}{;}{a}{,}{,}
\bibliographystyle{plainnat}

\frenchspacing

%\pagestyle{empty}

